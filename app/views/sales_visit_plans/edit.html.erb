<%# <div class="container-fluid">
  <h1 class="h3 mb-4 text-gray-800">Form Rencana Kunjungan Sales</h1>
  <hr>
  <%= render "form" %>
<%# </div> %>

<div class="container-fluid">
  <%# <h1 class="h2 text-primary text-center font-weight-bolder">Form Pekerja</h1> %>
  <%# <hr> %>
  <ul class="nav nav-tabs" id="myTab" role="tablist">
    <li class="nav-item" role="presentation">
      <button class="nav-link active" id="home-tab" data-bs-toggle="tab" data-bs-target="#home" type="button" role="tab" aria-controls="home" aria-selected="true">Rencana Kerja Sales</button>
    </li>
    <li class="nav-item ml-2" role="presentation">
      <% if params[:id].present? %>
        <button class="nav-link" id="kesehatan-tab" data-bs-toggle="tab" data-bs-target="#kesehatan" type="button" role="tab" aria-controls="kesehatan" aria-selected="false"><a href="<%= realisasi_activity_sale_path(params[:id])%>" style="text-decoration: none; color: black;">Realisasi Kerja Sales</a></button>
      <% else %>
        <button class="nav-link" id="profile-tab" data-bs-toggle="tab" data-bs-target="#profile" type="button" role="tab" aria-controls="profile" aria-selected="false">Realisasi Kerja Sales</button>
      <% end %>
    </li>
  </ul>
  <div class="tab-content ml-2" id="myTabContent">
    <div class="tab-pane fade show active mt-2" id="home" role="tabpanel" aria-labelledby="home-tab">
      <%= render "form" %>
    </div>
  </div>
</div>