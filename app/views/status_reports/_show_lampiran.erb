   
        <div class="card shadow mb-4 " id="form-catatan" >
          <% if @status_report.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@status_report.errors.count, "error") %> prohibited this site_project from being saved:</h2>
              <ul>
              <% @status_report.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
              </ul>
            </div>
          <% end %>
          
          <div class="card-header py-3">
            <div class="row justify-content-between">
              <div class="col align-items-center pt-2">
                <h6 class="font-weight-bold">Lampiran </h6>
              </div>
            </div>
          </div>

          <div class="card-body ">
            <table class="table table-bordered">
              <tbody>
                <div class="form-row">
                  <% @status_report.realization_visit_plan.sales_visit_plan.file_lampiran.each do |lampiran| %>
                    <%= image_tag(lampiran, size: "350x250", class:"img-fluid rounded mx-auto d-block")%>
                  <%end%>
                </div>
              </tbody>
            </table>
          </div>
        </div> 

         <div class=" py-3 ">
            <a href="<%= status_laporan_activity_sales_path %>" class="btn btn-info pull-right ml-2">Kembali</a>
            <%# <button type="submit" class="btn btn-primary px-3 pull-right">Submit</button> %>
         </div>