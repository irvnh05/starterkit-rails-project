<div class="row" style="justify-content: center;" >
  <div class="col-lg-12">
    <div class="card">
      <div class="card-body">
        <%= form_for @work_unit do |f| %>
          <% if @work_unit.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@work_unit.errors.count, "error") %> prohibited this work_unit from being saved:</h2>

              <ul>
              <% @work_unit.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
              </ul>
            </div>
          <% end %>

          <div class="form-group">
            <label>Satuan Kerja</label>
            <%= f.text_field :name, class: 'form-control' %>
          </div>
        
          <div class="form-group">
            <button type="submit" class="btn btn-primary px-3 pull-right">Submit</button>
            <a href="<%= work_units_path %>" class="btn btn-light px-3 pull-left">Cancel</a>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>